# Anong Ganap? Mobile App

React Native app with Expo Router and NativeWind (Tailwind CSS).

## Setup

```bash
cd mobile
npm install
npm start
```

## Run on device

- iOS: `npm run ios`
- Android: `npm run android`
- Web: `npm run web`
